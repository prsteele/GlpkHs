#! /usr/bin/env nix-shell
#! nix-shell --pure --quiet -i bash ../nix/ci.nix

echo "#"
echo "# Verifying nix/glpk-headers-haskell.nix is up to date..."
echo "#"
./ci/verify-project-nix

echo "#"
echo "# Running tests..."
echo "#"
./ci/test